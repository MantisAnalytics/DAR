# Google Cloud Configuration
# Replace with your actual Google Cloud Project ID (not the display name)
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud region for Vertex AI services
GOOGLE_CLOUD_LOCATION=us-east1 # put your region

# BigQuery dataset name for AI operations
# This dataset must already exist in your project
# You can get an example dataset to load to BigQuery here: https://www.kaggle.com/datasets/viktoriaskorik/incidents-dataset
GOOGLE_BQ_DATASET=kaggle_BQ_AI

# Enable Vertex AI for Generative AI (1 = enabled, 0 = disabled)
# Keep as 1 to use Google's managed AI services
GOOGLE_GENAI_USE_VERTEXAI=1

# Full BigQuery connection resource path
# Format: projects/PROJECT_ID/locations/LOCATION/connections/CONNECTION_NAME
# Find in: BigQuery Console > External connections > Copy connection ID
# Replace 'bq-project-name', 'your-chosen-location', and 'bg-dataset-name' with your values
GOOGLE_BQ_CONNECTION_ID=projects/bq-project-name/locations/your-chosen-location/connections/bg-dataset-name

# AI model for BigQuery ML operations
BQ_MODEL=gemini-2.5-flash

# Vertex AI Code Interpreter extension resource path (optional)
# Format: projects/PROJECT_ID/locations/LOCATION/extensions/EXTENSION_ID
# Find in: Vertex AI Console > Extensions > Copy full resource name
# Only needed if using code interpretation features
CODE_INTERPRETER_EXTENSION_NAME=projects/bq-project-name/locations/your-chosen-location/extensions/1234567890

# Path to your Google Cloud Service Account JSON key file
# Required permissions are described in README.md
# Security: Store outside project directory, never commit to version control
GOOGLE_APPLICATION_CREDENTIALS=path-to-your-service-account-json